# PDF Generator Workspace

This folder is reserved for future PDF generation work in this project.

## Notes
- `dompdf` is possible if a PHP-based backend or standalone PHP handler is added.
- For the current HTML/CSS/JavaScript setup, client-side options like `jsPDF`, `html2pdf.js`, or `pdf-lib` may be simpler depending on the PDF layout and export needs.

Add implementation files here later.
